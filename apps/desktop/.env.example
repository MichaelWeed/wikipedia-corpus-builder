# Optional override — CORPUSSIEVE_ENGINE_DIR is resolved at compile time
# relative to src-tauri by default (see src-tauri/src/engine.rs). Set this
# only for non-standard checkout layouts. Note: .env is NOT auto-loaded at
# runtime; export this in your shell before `cargo tauri dev` if you need it.
CORPUSSIEVE_ENGINE_DIR=../../../engine
