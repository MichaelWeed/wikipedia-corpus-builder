[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "corpussieve"
version = "0.1.0.dev0"
description = "Deterministic domain-specific Wikitext corpus compiler"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
authors = [
    { name = "CorpusSieve contributors" }
]
dependencies = [
    "pydantic>=2.7",
    "typer>=0.12",
    "rich>=13",
    "httpx>=0.27",
    "pyyaml>=6",
    "zstandard>=0.22",
    "mwparserfromhell>=0.6",
    "platformdirs>=4",
    "keyring>=25",
]

[project.scripts]
corpussieve = "corpussieve.cli.main:app_entry"

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-cov",
    "ruff>=0.5",
    "mypy>=1.10",
    "respx",
]

[tool.hatch.build.targets.wheel]
packages = ["src/corpussieve"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "A", "C4", "SIM", "ARG"]
ignore = []

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src"]
show_error_codes = true
pretty = true

[[tool.mypy.overrides]]
module = "tests.*"
strict = false
