## Description

Summary of changes included in this PR.

## Related Chunk / Issue

- Chunk ID: `P0.X`
- Fixes #

## Checklist

- [ ] All commits are signed off (`Signed-off-by: Author <email>`)
- [ ] Commit message titles use the chunk ID prefix (e.g., `P1.3: categorylinks SQL ingestion`)
- [ ] `cd engine && uv run ruff check . && uv run ruff format --check .` passes
- [ ] `cd engine && uv run mypy src` passes
- [ ] `cd engine && uv run pytest -q` passes
